[project]
# please adapt to package name
name = "ets"
version = "0.1.0"
description = "EM-Transformation-Service - a service that transforms Experimental Metadata"
dependencies = [
    "typer >= 0.19.1",
    "ghga-service-commons[api] >= 5.0",
    "ghga-event-schemas >= 10.0",
    "hexkit[akafka,s3,mongodb] >= 7.0",
    "metldata >= 5.1.1",
]

[project.urls]
# please adapt to package name
Repository = "https://github.com/ghga-de/em-transformation-service"

[project.scripts]
# please adapt to package name
ets = "ets.__main__:run"
